# KorDoc AI — 사용 설명서

## 한눈에 보기

```
앱 실행 → 파일 선택 → 액션 선택 → 처리 → 결과 확인
```

데스크톱 앱 하나로 HWP/HWPX/PDF/XLSX/DOCX 문서를 마크다운으로 변환하고, AI 요약·비교·OCR까지 처리합니다.

---

## 1. 준비물

| 항목 | 용도 | 비고 |
|------|------|------|
| KorDoc AI 설치 파일 (MSI) | 앱 설치 | Windows 10/11 |
| Node.js 20+ | 엔진 실행 | [nodejs.org](https://nodejs.org/) |
| Gemini API 키 | AI 기능 (OCR, 요약) | [Google AI Studio](https://aistudio.google.com/)에서 무료 발급 |

> 한컴오피스, MS Office 등 **별도 프로그램 설치 불필요** — kordoc이 모든 문서를 직접 파싱합니다.

---

## 2. 설치 및 최초 설정

### 설치

1. `KorDoc AI_x.x.x_x64_ko-KR.msi` 실행
2. 설치 마법사 완료
3. 바탕화면 또는 시작 메뉴에서 **KorDoc AI** 실행

### API 키 설정

1. 좌측 하단 **설정** 클릭
2. **Gemini API 키** 입력
3. 하단 상태바에서 엔진 상태 "Ready" 확인

> API 키는 `settings.yaml`에 로컬 저장됩니다. 공유 PC에서는 사용 후 키를 삭제하세요.

---

## 3. 화면 구성

```
┌──────────┬──────────────────────────────────────┐
│ KorDoc   │ 변환할 문서를 선택하세요                │
│  AI      │                                      │
│          │  ① 파일 선택  ② 변환 중  ③ 완료       │
│ ──────── │                                      │
│ 도구      │  ┌─ 파일 가져오기 ─────────────────┐  │
│ 문서 변환  │  │ 파일을 드래그하거나 선택하세요     │  │
│          │  │                                │  │
│ ──────── │  │   [파일 선택] [폴더 선택]         │  │
│ 시스템    │  └────────────────────────────────┘  │
│ 설정     │                                      │
│ 도움말   │  ┌─ 액션 선택 ─────────────────────┐  │
│          │  │ 변환 · OCR · 요약 · 비교 · ...   │  │
│          │  └────────────────────────────────┘  │
├──────────┴──────────────────────────────────────┤
│ 엔진: Ready │ 대기 중                            │
└─────────────────────────────────────────────────┘
```

| 영역 | 역할 |
|------|------|
| **사이드바** | 문서 변환 도구, 설정·도움말 접근 |
| **파이프라인 스테퍼** | 현재 처리 단계 시각적 표시 |
| **메인 영역** | 파일 선택 → 액션 선택 → 진행 → 결과 |
| **상태바** | 엔진 상태, 진행 현황, 경과 시간 |

---

## 4. 지원 파일 형식

| 형식 | 엔진 | 한컴/MS Office 필요 |
|------|------|:---:|
| HWP (구형 .hwp) | kordoc 직접 파싱 | 불필요 |
| HWPX (.hwpx) | kordoc 직접 파싱 | 불필요 |
| PDF | kordoc + Gemini Vision (이미지 PDF) | 불필요 |
| XLSX | kordoc 직접 파싱 | 불필요 |
| DOCX | kordoc 직접 파싱 | 불필요 |
| TXT/MD | 그대로 읽기 | 불필요 |

---

## 5. 사용 방법

### 기본 흐름

1. **파일 선택**
   - [파일 선택] 또는 [폴더 선택] 버튼 클릭
   - 또는 파일을 앱 창에 드래그앤드롭
   - 파일 타입별 배지로 구분 (HWP, PDF, XLSX 등)

2. **액션 선택** (파일 추가 후 하단에 표시)
   - 마크다운 변환, AI OCR, AI 요약, 문서 비교, 표 추출, 양식 추출, HWPX 생성, 문서 병합, 영수증 스캔

3. **처리 진행**
   - 진행률과 로그가 실시간 표시
   - [취소] 버튼으로 안전 중단

4. **결과 확인**
   - 성공/실패 건수 표시
   - [출력 폴더 열기]로 결과물 확인
   - [처음으로]로 새 작업 시작

### 10개 기능 상세

| 기능 | 설명 | API 필요 |
|------|------|:---:|
| 마크다운 변환 | HWP/HWPX/PDF/XLSX/DOCX → 마크다운. 표·리스트·제목 구조 유지 | 아니오 |
| 배치 변환 | 여러 파일 순차 변환 + 진행률 | 아니오 |
| AI OCR | 이미지 PDF를 Gemini Vision으로 텍스트 인식 | 예 |
| AI 요약 | 문서를 AI가 핵심 내용으로 요약 | 예 |
| 문서 비교 | 두 문서의 차이점 → 신구대조표 | 아니오 |
| 표 추출 | 문서에서 표만 추출 → 마크다운 테이블 | 아니오 |
| 양식 추출 | 신청서/보고서 양식 필드 자동 추출 | 아니오 |
| HWPX 생성 | 마크다운 → HWPX 역변환 | 아니오 |
| 문서 병합 | 여러 문서를 하나의 마크다운으로 병합 | 아니오 |
| 영수증 스캔 | 영수증 이미지 → 날짜/금액/항목 JSON 구조화 | 예 |

---

## 6. 설정

### 설정 모달

| 항목 | 설명 |
|------|------|
| Gemini API 키 | Google AI Studio에서 발급받은 키 |
| OCR 모델 | 기본: gemini-3-flash-preview |
| 분석 모델 | 기본: gemini-3-flash-preview |
| 모드 | 온라인 (AI 기능 사용) / 오프라인 (로컬 변환만) |
| 출력 위치 | 변환 결과 저장 경로 |
| 테마 | 라이트/다크 모드 전환 |

### YAML 설정 (고급)

`node-sidecar/config/settings.yaml`에서 세부 옵션 조정:

```yaml
gemini:
  model: gemini-3-flash-preview
  lite_model: gemini-3-flash-preview
  api_key: ""
  proxy_url: ""
  max_retries: 2
  timeout_ms: 60000
  mode: online

convert:
  output_dir: ""
  ocr_fallback: true

general:
  log_level: info
```

---

## 7. 문제 해결

| 증상 | 원인 | 해결 |
|------|------|------|
| "엔진을 시작할 수 없습니다" | Node.js 미설치 | Node.js 20+ 설치 |
| "엔진이 응답하지 않습니다" | sidecar 프로세스 문제 | 앱 재시작 (자동 재시작 3회 시도) |
| HWP 변환 실패 | 파일 손상 또는 암호화 | 다른 파일로 시도. 암호 해제 후 재시도 |
| 이미지 PDF 빈 텍스트 | API 키 미설정 | 설정에서 API 키 입력 + 온라인 모드 확인 |
| 429 Too Many Requests | API 호출 한도 | 자동 재시도됨. 잠시 후 재시도 |
| 표가 깨짐 | 복잡한 병합 셀 | '표 추출' 기능을 별도로 사용 |

---

## 8. 키보드 단축키

| 키 | 동작 |
|----|------|
| `Tab` / `Shift+Tab` | 포커스 이동 |
| `Enter` | 버튼 활성화 |
| `Esc` | 모달/투어 닫기 |

---

## 9. 기술 아키텍처

```
┌─────────────────────────────────┐
│  React 19 + Tailwind CSS        │  ← 프론트엔드
│  (Vite, TypeScript)             │
├─────────────────────────────────┤
│  Tauri 2.10 (Rust)              │  ← IPC 브릿지
│  JSON-RPC 2.0 whitelist         │
├─────────────────────────────────┤
│  Node.js Sidecar                │  ← 문서 처리 + AI
│  kordoc (로컬 파싱)              │
│  Gemini API (OCR/요약/스캔)      │
└─────────────────────────────────┘
```

- **프론트엔드**: React 컴포넌트 → Tauri IPC → Rust 커맨드
- **Rust 백엔드**: SidecarManager가 Node.js 프로세스 관리, JSON-RPC 통신
- **Node.js Sidecar**: stdin/stdout JSON-RPC 수신, kordoc + Gemini API 실행
