# EduPlan AI v2 — 사용 설명서

## 한눈에 보기

```
앱 실행 → 파일 선택 → OCR 실행 → AI 태그 확인 → 추출/분석 → 결과 확인
```

데스크톱 앱 하나로 문서 변환(MVP 1), 페이지 추출(MVP 2), 교육과정 분석(MVP 3)을 한 번에 처리합니다.

---

## v4에서 v2로 무엇이 달라졌는가

### 아키텍처 변경

| 항목 | v4 (이전) | v2 (현재) | 이유 |
|------|-----------|-----------|------|
| 프레임워크 | Python Tkinter | **Tauri + React + Python** | 모던 UI/UX, 크로스플랫폼 |
| 프론트엔드 | Python 위젯 | **React 19 + Tailwind CSS** | 반응형 디자인, 다크모드 |
| 백엔드 | Python 직접 실행 | **Rust → Python Sidecar** | 안정성, 비동기 처리 |
| 빌드 | PyInstaller exe | **MSI 설치 프로그램** | Windows 정식 설치 파일 |
| AI 파이프라인 | 탭별 수동 실행 | **자동 OCR → 태깅 → 추출 → 분석** | 원클릭 처리 |

### 핵심 개선

- **AI 자동 태깅**: Gemini Vision이 PDF 페이지를 테마별로 자동 분류
- **태그 리뷰**: AI 분류 결과를 수동으로 확인/수정 후 추출
- **온보딩 가이드**: 처음 사용 시 자동 투어로 UI 안내
- **접근성**: 키보드 네비게이션, 스크린리더, 다크모드 지원
- **자동 복구**: Python Sidecar 프로세스 크래시 시 자동 재시작

---

## 1. 준비물

### 반드시 필요한 것

| 항목 | 용도 | 비고 |
|------|------|------|
| EduPlan AI 설치 파일 (MSI) | 앱 설치 | Windows 10/11 |
| Gemini API 키 | AI 문서 인식 | [Google AI Studio](https://aistudio.google.com/)에서 무료 발급 |

### MVP별 추가 필요

| MVP | 추가 필요 | 설치 방법 |
|-----|-----------|-----------|
| MVP 1 | 한컴오피스 (HWP 변환) | PC에 설치되어 있어야 함 |
| MVP 2 | 없음 | AI 태그 기반 오프라인 추출 |
| MVP 3 | 없음 | MVP 1과 동일한 API 키 사용 |

---

## 2. 설치 및 최초 설정

### 설치

1. `EduPlan AI_1.0.0_x64_ko-KR.msi` 실행
2. 설치 마법사 완료
3. 바탕화면 또는 시작 메뉴에서 **EduPlan AI** 실행

### API 키 설정

1. 좌측 사이드바에서 **설정** 클릭
2. **Gemini API 키** 입력
3. 하단 상태바에서 "API 키: 설정됨" 확인

> API 키는 `.env` 파일에 암호화 없이 저장됩니다. 공유 PC에서는 사용 후 삭제를 권장합니다.

---

## 3. 화면 구성

```
┌──────────┬──────────────────────────────────────┐
│ EduPlan  │ 처리할 문서를 선택하세요                │
│  AI      │                                      │
│          │  ① 파일 선택  ② OCR  ③ 태그리뷰  ④ 완료│
│ ──────── │                                      │
│ 기능      │  ┌─ 파일 가져오기 ─────────────────┐  │
│ 문서처리   │  │ 📎 파일을 드래그하거나 선택하세요  │  │
│ 자료수집   │  │                                │  │
│          │  │   [파일 선택]                     │  │
│ ──────── │  └────────────────────────────────┘  │
│ 시스템    │                                      │
│ 설정     │  [OCR 시작]                           │
│ 도움말   │                                       │
├──────────┴──────────────────────────────────────┤
│ API 키: 설정됨 │ 대기 중                          │
└─────────────────────────────────────────────────┘
```

### 구성 요소

| 영역 | 역할 |
|------|------|
| **사이드바** | 문서 처리 / 자료 수집 전환, 설정·도움말 접근 |
| **파이프라인 스테퍼** | 현재 처리 단계 시각적 표시 (① → ② → ③ → ④) |
| **메인 영역** | 각 단계별 UI (파일 선택, OCR 진행, 태그 리뷰, 결과) |
| **상태바** | API 키 상태, 진행 현황, 경과 시간 |
| **토스트 알림** | 성공/실패/정보 팝업 알림 |

---

## 4. 사용 방법

### 전체 파이프라인 (MVP 1 + 2 + 3)

1. **파일 선택**
   - [파일 선택] 버튼 또는 드래그 앤 드롭으로 HWP/PDF 파일 추가
   - 파일 타입별 뱃지로 구분 (HWP, PDF)

2. **[OCR 시작]** 클릭
   - Gemini Vision AI가 문서의 텍스트와 표를 인식
   - 진행률과 로그가 실시간 표시
   - 필요 시 [취소] 가능

3. **AI 태그 리뷰**
   - OCR 완료 후 AI가 각 페이지를 테마별로 자동 분류
   - 테마: 학급수 및 학생수, 각종 면적, 사업 유형, 기간, 발주 방식, 사업비, 스페이스프로그램, 설문, 기타
   - 잘못된 태그는 드롭다운으로 수정
   - [확인] 클릭으로 다음 단계 진행

4. **추출 및 분석**
   - 확인된 태그 기반으로 테마별 PDF 자동 추출
   - 교육과정 텍스트 요약 + 통합 분석 리포트 생성

5. **결과 확인**
   - 성공/실패 건수 표시
   - [출력 폴더 열기]로 결과물 바로 확인
   - [처음으로]로 새 작업 시작

### 자료 수집 도구 (웹 도구)

사이드바에서 **자료 수집** 탭 선택 시 8가지 웹 자동화 도구 사용 가능:

| 도구 | 기능 |
|------|------|
| 학교알리미 | 학교 기본 정보 조회 |
| 세움터 | 건축물 정보 조회 |
| 토지이음 | 토지 정보 조회 |
| 인구통계 | 지역별 인구 통계 |
| 유산정보 | 문화재 정보 조회 |
| 설계대가 | 설계비 산출 |
| 학구도안내 | 학교 배정구역 조회 |

> 웹 도구는 Playwright(선택 설치)가 필요합니다.

---

## 5. 설정

### 설정 모달

| 항목 | 설명 |
|------|------|
| Gemini API 키 | Google AI Studio에서 발급받은 키 |
| OCR 모델 | 기본: gemini-3-flash-preview |
| 분석 모델 | 기본: gemini-3.1-flash-lite-preview |
| 테마 | 라이트/다크 모드 전환 |

### YAML 설정 (고급)

`python-sidecar/config/settings.yaml`에서 세부 옵션 조정:

```yaml
mvp1:
  model_name: "gemini-3-flash-preview"
  chunk_size: 20      # 한 번에 처리할 페이지 수
  max_workers: 3      # 동시 처리 워커 수

mvp3:
  model_name: "gemini-3.1-flash-lite-preview"
  num_variants: 2     # 분석 결과 변형 수
```

---

## 6. 문제 해결

| 증상 | 원인 | 해결 |
|------|------|------|
| 하단 상태바 "API 키: 미설정" | API 키 미입력 | 설정에서 입력 |
| "Sidecar 연결 실패" | Python 프로세스 미시작 | 앱 재시작 (자동 재시작 3회까지 시도) |
| HWP 변환 실패 | 한컴오피스 미설치 | 한컴오피스 설치 필요 |
| 429 Too Many Requests | API 호출 한도 | 자동 재시도됨. settings.yaml에서 chunk_size 낮추기 |
| 메모리 부족 | PDF 페이지 과다 | settings.yaml에서 chunk_size를 10으로 |
| 태깅 결과가 부정확 | AI 분류 한계 | 태그 리뷰 단계에서 수동 수정 |

---

## 7. 키보드 단축키

| 키 | 동작 |
|----|------|
| `Tab` / `Shift+Tab` | 포커스 이동 |
| `Enter` | 버튼 활성화 |
| `Esc` | 모달/투어 닫기 |

---

## 8. 기술 아키텍처

```
┌─────────────────────────────────┐
│  React 19 + Tailwind CSS        │  ← 프론트엔드
│  (Vite, TypeScript)             │
├─────────────────────────────────┤
│  Tauri 2.10 (Rust)              │  ← IPC 브릿지
│  JSON-RPC 2.0                   │
├─────────────────────────────────┤
│  Python Sidecar                 │  ← AI/문서처리
│  (Gemini API, PyMuPDF, pypdf)   │
└─────────────────────────────────┘
```

- **프론트엔드**: React 컴포넌트, Tauri API로 Rust 호출
- **Rust 백엔드**: SidecarManager가 Python 프로세스 관리, JSON-RPC 통신
- **Python Sidecar**: stdin/stdout으로 JSON-RPC 수신, MVP 1/2/3 파이프라인 실행
