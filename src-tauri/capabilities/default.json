{
  "identifier": "default",
  "description": "기본 권한 설정 (최소 권한 원칙)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "shell:allow-open"
  ]
}
